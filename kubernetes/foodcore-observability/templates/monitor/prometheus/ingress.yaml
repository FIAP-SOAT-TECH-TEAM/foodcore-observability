apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "prometheus.fullname" . }}-ingress
  namespace: {{ .Values.namespace.monitor.name }}
  labels:
    app: {{ include "prometheus.name" . }}
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
    appgw.ingress.kubernetes.io/use-private-ip: "false"
    appgw.ingress.kubernetes.io/health-probe-path: {{ .Values.prometheus.ingress.path | quote }}
    appgw.ingress.kubernetes.io/health-probe-port: {{ .Values.prometheus.ports.port | quote }}

spec:
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}

  rules:
  - host: {{ (index .Values.ingress.hosts 0).host | quote }}
    http:
      paths:
      - path: {{ .Values.prometheus.ingress.path }}
        pathType: {{ .Values.prometheus.ingress.pathType }}
        backend:
          service:
            name: {{ include "prometheus.fullname" . }}-service
            port: 
              number: {{ .Values.prometheus.ports.port }}